# OpenAI API Key (required for GPT models)
OPENAI_API_KEY=your_openai_api_key_here

# Alternative: Use free open-source models (no API key needed)
# Set USE_LOCAL_MODEL=true to use sentence-transformers for generation
USE_LOCAL_MODEL=false

# Vector store configuration
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
